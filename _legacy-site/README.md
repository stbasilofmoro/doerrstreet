# Archived — previous site

Everything in this folder is the site as it was before this branch, moved
here as-is (not deleted) so nothing is lost and the history stays reversible.

It includes the ~20 SEO landing pages (`track-inspection-*.html`,
`industrial-*.html`, etc.) that aren't part of the new build yet — those
need a decision on whether to keep, redirect, or retire before launch.

Once the new site is live and this folder's contents are confirmed no
longer needed, it can be deleted in a follow-up commit.
